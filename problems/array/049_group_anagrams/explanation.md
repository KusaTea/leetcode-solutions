# Explanation

## Pattern
Sorting, hashmap

## Idea
Anagrams have common soted string. Thus, we can use sorted strings as keys in hashmap woth arrays of initial strings as values. Function must return the hashtable's values formatted as a array.

## Complexity
- Time: O(m * nlogn)
- Space: O(m*n)
